# User Simulation Configuration
# Settings for simulating realistic user behavior

# User Load Configuration
light.load.users=10
light.load.rampup=30
light.load.duration=180

medium.load.users=50
medium.load.rampup=120
medium.load.duration=600

heavy.load.users=100
heavy.load.rampup=300
heavy.load.duration=1200

stress.load.users=200
stress.load.rampup=600
stress.load.duration=1800

# Think Times (milliseconds)
min.think.time=500
max.think.time=3000
avg.think.time=1500

# Connection Settings
connection.timeout=30000
response.timeout=60000

# Request Settings
follow.redirects=true
use.keepalive=true
concurrent.pool=6